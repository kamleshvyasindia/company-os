# Marketing OS

Installed loops live here, one canvas per workflow, finished work in `outputs/`.

- `user-signal-to-content.md` — the worked example and recommended first loop. Users tell you what to write; this loop listens, drafts, and gates.

Next candidates (install when the first runs boringly well): launch-announcement loop (ship → changelog → post drafts across channels), distribution repurposer (long-form → threads/shorts/newsletter), experiment tracker (every title/hook/post as a logged experiment).
